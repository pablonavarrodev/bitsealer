services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      # Puedes sobreescribir la API al construir la imagen
      # args:
      #   VITE_API_BASE: "http://localhost:8080/api"
    #ports:
    #  - "5173:80"
